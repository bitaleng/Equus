안녕하세요.

현재 "He's 입실관리매니저" (V2 스킨, 휴게텔 입실관리 PWA) 프로젝트를 
실제 매장에서 사용 중입니다.

앞으로 버그 수정과 기능 추가를 Cursor AI 에이전트로 진행하려고 하는데,
지금 Replit에서 내보낸 소스 폴더를 확인해 보니 개발에 필요한 파일이 
일부 빠져 있는 것 같습니다.

Cursor에서 바로 개발·빌드·배포까지 이어갈 수 있도록,
아래 내용을 포함한 "완전한 개발용 소스 패키지"를 준비해 주세요.

---

【1. 반드시 포함해 주세요】

1) Git 저장소 전체
   - .git 폴더 포함 (커밋 이력이 있어야 변경 추적이 가능합니다)
   - 또는 GitHub/GitLab 등 원격 저장소 접근 권한

2) 스킨 이미지 파일 (현재 manifest.json만 있고 PNG가 없음)
   - client/skins/v1/ : favicon.png, icon-192.png, icon-512.png, icon-1024.png
   - client/skins/v2/ : favicon.png, icon-192.png, icon-512.png, icon-1024.png

3) 데이터베이스 설정 파일
   - drizzle.config.ts (없어서 npm run db:push 가 동작하지 않음)
   - DB 마이그레이션 파일이 있다면 migrations 폴더 전체

4) 빌드/배포 스크립트
   - scripts/build-and-package.js (README에서 언급되지만 없음)
   - build-netlify.sh 외에 사용하는 다른 빌드 스크립트가 있다면 전부

5) 문서
   - OFFLINE_LICENSE_GUIDE.md (replit.md에서 참조하지만 없음)
   - 개발 환경 세팅 방법 (Windows PC 기준으로 npm install, dev 실행, build, Netlify 배포 순서)
   - .env 예시 파일 (.env.example) — 실제 비밀값은 제외하고 변수 이름과 설명만

6) 환경 변수 목록과 설명
   현재 .env에는 VITE_* 변수만 있습니다.
   아래 변수들도 필요한지, 각각 무엇인지 알려주세요:
   - DATABASE_URL
   - ADMIN_KEY
   - VITE_SKIN, VITE_APP_NAME, VITE_APP_SHORT_NAME, VITE_APP_DESCRIPTION, VITE_LICENSE_STORAGE_KEY
   - 그 외 필요한 모든 환경 변수

7) attached_assets 폴더
   - vite.config.ts에 alias로 등록되어 있는데 폴더 자체가 없습니다.
   - 사용 중이라면 폴더 전체를 포함해 주세요.

---

【2. 현재 운영 방식 확인 요청】

저는 개발 경험이 없는 비개발자입니다.
아래를 쉬운 말로 알려주세요:

1) 지금 실제 매장에서 쓰는 버전은 어떤 방식으로 배포되어 있나요?
   - Netlify 정적 배포(PWA 오프라인)인지
   - Replit 서버 + PostgreSQL(스마트 락커 연동)인지
   - 둘 다 쓰는지

2) 버그 수정·기능 추가 후 배포는 어떤 순서로 하나요?
   (예: 코드 수정 → 빌드 → Netlify zip 업로드)

3) V1(에쿠스/EQUS)과 V2(He's/HIZZ) 중 
   현재 실사용 중인 건 V2 맞나요?

4) 라이선스 키 생성은 license-generator.html 로 로컬에서 하는 방식이 맞나요?
   라이선스 서명에 쓰는 비밀키(Secret) 관리 방법도 알려주세요.

---

【3. Windows PC에서 개발할 예정】

개발 PC는 Windows입니다.
아래도 확인·수정해 주세요:

1) package.json의 dev/build 스크립트가 Windows에서도 동작하는지
   (현재 NODE_ENV=development 같은 Unix 방식이라 Windows에서 안 될 수 있음)

2) Windows에서 로컬 개발 서버 실행 방법
3) Windows에서 Netlify 배포용 zip 빌드 방법
   (build-netlify.sh는 bash 기준이라 Windows 대안도 필요)

---

【4. 받고 싶은 최종 결과물 형태】

아래 중 하나로 정리해 주세요:

□ 방법 A: GitHub 저장소 링크 (가장 선호)
□ 방법 B: zip 파일 (node_modules, dist 제외 — npm install / npm run build로 생성)

포함해야 할 것:
- 전체 소스 코드
- 스킨 이미지
- DB 설정/마이그레이션
- .env.example
- 개발·빌드·배포 가이드 (한국어, 비개발자도 따라 할 수 있는 수준)
- 현재 실사용 중인 V2(He's) 기준 설정값

제외해도 되는 것:
- node_modules (npm install로 설치)
- dist, client/dist (빌드로 생성)

---

【5. 추가로 알려주시면 좋은 것】

- 알려진 버그 목록이나 TODO
- 최근 변경 사항 (특히 localDb.ts 마이그레이션 step 번호 등)
- 스마트 락커 하드웨어 연동을 실제로 쓰고 있는지 여부
- CCTV 기능 사용 여부

감사합니다.